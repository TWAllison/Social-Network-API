const { User, Thought } = require('../models');

const userController = {
    // get all users

    // get single user

    // new user

    //update user

    // delete user 

    // add a friend 

    // remove a friend
};

module.exports = userController;